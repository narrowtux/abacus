# 2.2.0

 - added support to list literals (`[1, true, "string"])
 - added support for map literals (`{key: 1, 3: "string"}`) keys can be any expression like in elixir. Only atom keys can not be generated
